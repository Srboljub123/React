import { Col } from 'react-bootstrap'
import styled from 'styled-components'

export const TicketSectionContainer = styled(Col)`
    display: flex;
    justify-content: center;
    padding-right: 0;
    padding-left: 0;
`
